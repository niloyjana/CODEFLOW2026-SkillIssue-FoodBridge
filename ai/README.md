# AI / Machine Learning Developer Guide

This directory holds the predictive AI models and inference endpoints for waste prediction.

## Setup & Development
The AI developer will build waste prediction models (e.g., forecasting surplus/waste kg based on portions, seating capacity, time of day, and venue types).

### Planned Endpoints
- `POST /predict` (accepts venue parameters, returns `predictedWasteKg`)
